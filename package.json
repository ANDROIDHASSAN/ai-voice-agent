{
  "name": "ai-voice-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Vercel-only deploy: React frontend (client/) + Express backend as a serverless function (api/).",
  "scripts": {
    "vercel-build": "npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "googleapis": "^144.0.0",
    "helmet": "^7.1.0",
    "mongoose": "^8.6.0",
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
